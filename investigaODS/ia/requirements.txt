# Dependencias para Green Dream RAG Assistant
# API y servidor web
flask==3.0.0
flask-cors==4.0.0
python-dotenv==1.0.0

# Azure AI Foundry
azure-ai-inference==1.0.0b4
azure-core>=1.30.0,<2.0.0

# Procesamiento de datos
# (Removed numpy pin to avoid build-from-source issues on some PaaS builders)
gunicorn>=20.1.0